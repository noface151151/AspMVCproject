@using Model.EF
@model List<NhaXuatBan>
     @*    <ul id="submenu" class="nav navbar-nav">
        @foreach (var item in Model)
        {
            <li style="list-style:none">
                @Html.ActionLink(item.TenNXB, "SachNXB", "NhaXuatBan", new { @manxb = item.MaNXB }, null)
            </li>
        }

    </ul>*@  
<ul class="nav in">
    @foreach (var item in Model)
    {
        <li>
            @Html.ActionLink(item.TenNXB, "SachNXB", "NhaXuatBan", new { @manxb = item.MaNXB }, new { @id = "tenNXB" })
        </li>
    }
</ul>
<style>
    #tenNXB:hover{
        background-color:#ffd800;
    }
</style>